# Alacritty Configuration

I've included my alacritty configuration file for all to use. It uses the dracula color scheme and the Mononoki font. Copy the file to `.config/alacritty`
and save it as `alacritty.yml`.
